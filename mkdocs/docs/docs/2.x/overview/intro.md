# KoalaTs 2.x Overview

The full `2.x` documentation content will be migrated into the MkDocs platform in the next slice.

This placeholder keeps the landing-page CTA and future versioned URL contract stable:

- `/docs/2.x/overview/intro/`
