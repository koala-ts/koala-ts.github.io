name: Redeploy All Docs
description: >
  Transitional local action skeleton for redeploying all configured docs branches
  in order. This action will replace reusable-workflow fan-out during the
  workflow simplification sequence.

inputs:
  registry_json:
    description: Optional registry payload override for orchestration
    required: false

runs:
  using: composite
  steps:
    - shell: bash
      run: |
        echo "This is a scaffold action only."
        echo "Republish-all orchestration moves here in a later PR."
